#include <cstddef>
struct Elf{};

typedef void ** (*func) (std::size_t , char const *);

typedef double (Elf::*pf)(unsigned) const;

struct Unit{};

struct Archer{};

typedef Unit const * (Archer::*pm);

int main(){



}
